name: Hello Actions
on: push
jobs:
  build:
    name: Check Development Tools
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - run: echo "Hello World"
      - run: node --version
      - run: npm --version
      - run: yarn --version
