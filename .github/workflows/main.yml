name: Hello Actions
on: push
jobs:
  build:
    name: Hello world action
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v3
      - run: echo "Hello World"
      - run: node --version
      - run: npm --version
      - run: yarn --version
